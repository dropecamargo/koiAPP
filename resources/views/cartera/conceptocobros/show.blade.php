@extends('cartera.conceptocobros.main')

@section('breadcrumb')
    <li><a href="{{ route('conceptocobros.index')}}">Concepto de cobro</a></li>
    <li class="active">{{ $conceptocobro->id }}</li>
@stop

@section('module')
    <div class="box box-primary">
        <div class="box-body">
            <div class="row">
                <div class="form-group col-md-4">
                    <label class="control-label">Nombre</label>
                    <div>{{ $conceptocobro->conceptocob_nombre }}</div>
                </div>
                <div class="form-group col-md-2 col-xs-8 col-sm-3"><br>
                    <label class="checkbox-inline" for="conceptocob_activo">
                        <input type="checkbox" id="conceptocob_activo" name="conceptocob_activo" value="conceptocob_activo" disabled {{ $conceptocobro->conceptocob_activo ? 'checked': '' }}> Activo
                    </label>
                </div>
            </div>
        </div>
        <div class="box-footer with-border">
            <div class="row">
                <div class="col-md-2 col-md-offset-4 col-sm-6 col-xs-6 text-left">
                    <a href=" {{ route('conceptocobros.index') }}" class="btn btn-default btn-sm btn-block">{{ trans('app.comeback') }}</a>
                </div>
                <div class="col-md-2 col-sm-6 col-xs-6 text-right">
                    <a href=" {{ route('conceptocobros.edit', ['conceptocobros' => $conceptocobro->id])}}" class="btn btn-primary btn-sm btn-block"> {{trans('app.edit')}}</a>
                </div>
            </div>
        </div>
    </div>
@stop
